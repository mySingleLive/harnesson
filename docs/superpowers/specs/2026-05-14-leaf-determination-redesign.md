# 叶子节点判定逻辑重设计

日期：2026-05-14

## 背景

sync-specs skill 当前使用统一的「独立性三条件」判断叶子节点。在实际使用中，前后端代码的叶子判定逻辑差异较大，统一标准导致判定不够精准。

## 设计

### 新叶子判定标准

**前端（UI）叶子** — 满足任一即为叶子：
1. 用户触发的一次操作（点击按钮、展开/收起树、键盘输入等）所执行的单一功能
2. 足够简单、无复杂功能的组件
3. 多个相同类型的子组件或子功能可合并为一个（如表单中多个输入框的输入和校验合并为一个）

**后端叶子** — 满足任一即为叶子：
1. 由 API 触发的一个业务功能
2. 可被外部触发的功能（定时任务、Hooks、AI Function Call、Skills 等）

**跨前后端功能**：按子项目拆为前端节点和后端节点，共享同一父节点。

**无法确定时**：假定为非叶子，继续拆解子节点；若无法再拆解，则判定为叶子。

**Fallback**：前后端标准均无法判定时，旧的独立性三条件作为参考（不作为主判定依据）。

### 改动范围

#### 1. SKILL.md

**替换段落**：叶子节点判定（L35-L48），替换为新的前后端分别判定标准 + fallback。

**更新引用**（4 处）：

- Step 1 全量模式 4c：`用独立性三条件判断` → `按前后端叶子判定标准判断，无法确定时假定为非叶子并继续拆解`
- Step 1 叶子审计 5：`对枚举出的功能列表应用独立性三条件` → `按前后端叶子判定标准分析。无法确定时假定为非叶子并继续拆解；若无法再拆解则判定为叶子`
- Step 1 自检：`叶子节点是否通过独立性三条件审查` → `叶子节点是否通过前后端叶子判定标准审查`
- Step 3 叶子复核 6：`再次检查独立性三条件` → `按前后端判定标准检查，无法确定时假定为非叶子`

#### 2. node-identification-examples.md

**重写 3 个章节**：

- 「叶子判定核心规则」→ 按前后端分别列出的判定标准
- 「独立性判断对比示例」→ 替换为前后端叶子判定对比示例表格
- 「动词枚举检查示例」→ 替换为判定流程示例表格

**调整其余章节**：更新对旧规则的措辞引用，匹配新标准。

### 不变部分

- 节点识别原则（业务域/业务功能/技术模块三层判断）不变
- 正面示例、反面示例不变
- 叶子/非叶子节点示例、操作级拆分示例不变（仅调整措辞）
- 存储结构、节点 Schema 不变
